// Crea una función que reciba dos números como parámetros y devuelva el mayor de ellos.

let a = prompt("Ingrese un numero")
let b = prompt("Ingrese otro numero numero")

function mayor(a, b) {
  return Math.max(a, b)
}

alert(mayor(a, b))